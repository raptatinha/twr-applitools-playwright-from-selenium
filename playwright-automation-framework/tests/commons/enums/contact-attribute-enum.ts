export enum EAttributeType {
  STRING = "string",
  NUMBER = "number",
  DATE = "date",
  GEO = "geo",
  ARRAY = "array",
}

export enum EIndexedType {
  ONE = "1",
  ZERO = "0",
}

export enum ERetainOnDestroy {
  ANONYMIZED = "anonymized",
  UNCHANGED = "unchanged",
  NULL = "null",
}
